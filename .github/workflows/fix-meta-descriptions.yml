name: Fix Meta Descriptions

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node scripts/fix-meta-descriptions.mjs
      - run: node scripts/check-meta-descriptions.mjs
